# ============================================
# This Story - Production Environment
# ============================================
# This file is loaded automatically by Next.js when running `next start` or `next build`
# All secrets MUST be replaced with real values before deployment.

# Database (use managed PostgreSQL: Supabase, Neon, Railway, etc.)
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/thistory?sslmode=require"

# Auth.js (generate: openssl rand -base64 32)
NEXTAUTH_URL="https://your-domain.com"
AUTH_TRUST_HOST=true
AUTH_SECRET=""

# OpenAI
OPENAI_API_KEY=""

# Web Push (generate: npx web-push generate-vapid-keys)
NEXT_PUBLIC_VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""

# Cron (generate: openssl rand -base64 32)
CRON_SECRET=""

# Production settings
LOG_LEVEL="warn"
