# ============================================
# This Story - Environment Variables
# ============================================
# Copy to .env.local for local overrides, or use:
#   .env.development  (auto-loaded by `next dev`)
#   .env.production   (auto-loaded by `next build` / `next start`)
#
# Next.js env file loading order (highest priority first):
#   1. .env.local           (always, gitignored)
#   2. .env.[environment]   (.env.development or .env.production)
#   3. .env                 (always)

# Database
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/thistory"

# NextAuth.js (generate secret: openssl rand -base64 32)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET=""
AUTH_TRUST_HOST=true
AUTH_SECRET=""

# OpenAI
OPENAI_API_KEY=""

# Web Push (generate: npx web-push generate-vapid-keys)
NEXT_PUBLIC_VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""

# Cron Secret (generate: openssl rand -base64 32)
CRON_SECRET=""

# Logging (debug | info | warn | error)
LOG_LEVEL="info"
