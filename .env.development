# ============================================
# This Story - Development Environment
# ============================================
# This file is loaded automatically by Next.js when running `next dev`
# Local overrides: create .env.local (gitignored)

# Database (local Docker PostgreSQL)
DATABASE_URL="postgresql://thistory:thistory@localhost:5432/thistory"

# Auth.js
NEXTAUTH_URL="http://localhost:3000"
AUTH_TRUST_HOST=true
# Generate: openssl rand -base64 32 → put in .env.local
AUTH_SECRET=""

# OpenAI
OPENAI_API_KEY=""

# Web Push (generate: npx web-push generate-vapid-keys → put in .env.local)
NEXT_PUBLIC_VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""

# Cron (generate: openssl rand -base64 32 → put in .env.local)
CRON_SECRET=""

# Dev-only settings
LOG_LEVEL="debug"
